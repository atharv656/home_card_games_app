{
  "name": "card-game-app",
  "version": "1.0.0",
  "description": "A web app for playing classic card games with multiplayer support",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd frontend && npm run build",
    "build:all": "cd frontend && ./node_modules/.bin/vite build && cd ../backend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["card-games", "multiplayer", "web-app", "react", "socket.io"],
  "author": "",
  "license": "MIT"
} 